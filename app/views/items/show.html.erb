<div class="container">
  <img class="show_image mt-3 rounded-4"src="<%= @item.image_url %>" alt="a nice sports item" width="100%">
    <div class="header-show m-2">
      <h2><%= @item.name %></h2>
      <p><strong><%= @item.price %></strong>€ week</p>
    </div>
  <p><%= @item.description %></p>
  <p>Leased by <strong><%= "#{@item.user.first_name.capitalize} #{@item.user.last_name.capitalize}" %></strong></p>
  <%# TODO: Add link to the create a new reservation %>
    <%= simple_form_for([@item, @rental]) do |f| %>
        <%= f.input :start_date, as: :date, html5: true %>
        <%= f.input :end_date, as: :date, html5: true %>
        <%= f.submit class:"btn-create" %>
    <% end %>
</div>
<div class="banner">
  <%= link_to root_path do %>
     <i class="fa-solid fa-circle-chevron-left mt-2 mb-2"></i>
  <% end %>
</div>
